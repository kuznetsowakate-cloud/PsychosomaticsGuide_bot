# PsychosomaticsGuide Bot — правила работы

## После каждого изменения

После любого изменения кода необходимо сделать **commit и push**:

```
git add <изменённые файлы>
git commit -m "..."
git push
```

Это обеспечивает актуальность кода на Railway и сохраняет историю изменений.
